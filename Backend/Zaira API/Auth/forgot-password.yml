info:
  name: forgot-password
  type: http

http:
  method: POST
  url: http://localhost:5000/api/auth/forgot-password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
