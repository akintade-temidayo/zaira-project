info:
  name: verify-otp
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/auth/verify-otp
  body:
    type: json
    data: |-
      {
          "email": "temi@test.com",
          "otpCode": "932003"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
