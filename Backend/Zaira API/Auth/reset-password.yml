info:
  name: reset-password
  type: http
  seq: 1

http:
  method: POST
  url: |
    http://localhost:5000/api/auth/reset-password
  body:
    type: json
    data: |-
      {
          "email": "temi@test.com",
          "otpCode": "123456",
          "newPassword": "resetpassword789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
