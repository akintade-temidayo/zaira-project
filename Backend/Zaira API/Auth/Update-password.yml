info:
  name: Update-password
  type: http

http:
  method: PUT
  url: http://localhost:5000/api/auth/update-password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
