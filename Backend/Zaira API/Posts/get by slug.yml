info:
  name: get by slug
  type: http

http:
  method: GET
  url: http://localhost:5000/api/posts/jollof-rice-recipe
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
