info:
  name: get my posts
  type: http

http:
  method: GET
  url: http://localhost:5000/api/posts/my-posts
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5​