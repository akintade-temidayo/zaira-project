info:
  name: Update post
  type: http

http:
  method: PUT
  url: http://localhost:5000/api/posts/jollof-rice-recipe
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
